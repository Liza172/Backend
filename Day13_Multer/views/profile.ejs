<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://cdn.tailwindcss.com" ></script>
</head>
<body>
  <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
    <div class="w-full flex justify-end">
      <a class="bg-red-600 rounded-md px-3 py-2 text-sm md-5 inline-block" href="/logout">Log Out</a>
    </div>
  
    <!-- <form action="/profile" method="post"> -->
     <h3 class="text-3xl mb-3">Hello, <%= user.name %></h3>
     <h5>You can create a post.</h5>
     <form action="/post" method="post">
        <textarea placeholder="Whats on your mind" class="block p-3 outline-none w-1/4 bg-transparent border-2 border-zinc-500 text-white mt-4 rounded-md" name="content"></textarea>
        <input class="px-3 w-35 py-2 bg-blue-500 rounded-md block mt-4" type="submit" value="Create new post">

     </form>
     <div class="posts mt-20">
        <h3 class="text-zinc-400">Your Posts.</h3>
        <div class="postconatiner mt-5">
          <% user.posts.reverse().forEach(function(post){ %>
            <div class="post w-1/3 p-4 rounded-md border-[1px] border-zinc-500 bg-zinc-800 mt-4">
            <h4 class="text-blue-500 mb-2"><%= user.username %></h4>
            <p class="text-sm tracking-tight"><%= post.content %></p>
            <small class="mt-2 inline-block"><%= post.likes.length %></small>
            <div class="flex gap-4">
              <a class="text-blue-500 mt-2" href="/like/<%= post._id %>" >
                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
              </a> 
              <a class="text-zinc-500 mt-2" href="/edit/<%= post._id %>">edit</a>
            </div>
            </div>
          <% }) %>
          
        </div>
     </div>
   
  </div>
</body>
</html>